Create Factory
Create Token

To create a HTLC-ERC20O contract use approvalAndCall on Token with Factory as its destination and extrabyte data as your digest

To lock a HTLC-ERC20O contract to a destination, then call "lock" using the account you want to lock it to
A lock will run out after the time's up
