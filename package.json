{
    "name": "DATX02-18-07",
    "version": "0.0.1",
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
        "postinstall": "lerna bootstrap",
        "test": "lerna run elm:installBuild",
        "debug": "lerna run elm:installBuild && lerna run start --parallel"
    },
    "devDependencies": {
        "lerna": "^2.9.0"
    }
}
