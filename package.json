{
    "name": "DATX02-18-07",
    "version": "0.0.1",
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
        "postinstall": "lerna bootstrap --npm-client=yarn",
        "test": "lerna run test --stream",
        "debug": "lerna run elm:installBuild --stream && lerna run start --parallel"
    },
    "devDependencies": {
        "lerna": "^2.9.0",
        "yarn": "^1.5.1"
    },
    "dependencies": {
        "npm": "^5.8.0"
    }
}
